[pytest]
testpaths = tests
addopts = -ra --alluredir=allure-results
markers =
    api: REST API teaching scenarios
    ui: browser teaching scenarios
    extended: extended catalog scenarios selected by traceability ID
